<Window x:Class="YoutubePlaylistWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YoutubePlaylistWPF"
        xmlns:views="clr-namespace:YoutubePlaylistWPF.Views"
        xmlns:controllers="clr-namespace:YoutubePlaylistWPF.Controllers"
        mc:Ignorable="d"
        Title="WunderVision Youtube Playlist WPF" Height="640
        " Width="800">
    <Window.Resources>
        <controllers:PlaylistController x:Key="playlistController"/>
    </Window.Resources>
    <Grid DataContext="{Binding Source={StaticResource playlistController}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <views:Player Grid.Column="0" WebViewHTMLPath="YoutubePlayer.html"/>
        <views:Playlist Grid.Column="1"/>

    </Grid>
</Window>
